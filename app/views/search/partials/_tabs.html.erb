<div class="tabs">
  <ul class="nav nav-tabs" id="myTab" role="tablist">
  	<li class="nav-item">
      <a class="nav-link active" id="plats-vendus-tab" data-toggle="tab" href="#plats-vendus" role="tab" aria-controls="plats-vendus" aria-selected="false">Plats vendus </a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="plats-invendus-tab" data-toggle="tab" href="#plats-invendus" role="tab" aria-controls="plats-invendus" aria-selected="false">Plats invendus </a>
    </li>
  </ul>
<div class="tab-content" id="myTabContent">
	<!-- tabs plats vendus -->
	 <div class="tab-pane fade show active" id="plats-vendus" role="tabpanel" aria-labelledby="plats-vendus-tab">
	   <%= render "search/partials/subpartials/food_charts" %>
	  </div>

	<!-- tabs plats invendus -->
  	<div class="tab-pane fade show" id="plats-invendus" role="tabpanel" aria-labelledby="plats-invendus-tab">
   <%= render "search/partials/subpartials/unsold_food_pie" %>
  	</div>

  
</div>
</div>